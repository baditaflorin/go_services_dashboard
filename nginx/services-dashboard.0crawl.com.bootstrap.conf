server {
    listen 80;
    listen [::]:80;
    server_name services-dashboard.0crawl.com;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 200 "Bootstrap pending SSL generation";
    }
}
